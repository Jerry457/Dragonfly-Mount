local Assets = Assets
GLOBAL.setfenv(1, GLOBAL)
local fx = require("fx")

-- local assets = {
-- }

-- local data = {
--     {
--     },
-- }

-- for _, v in ipairs(data) do
--     table.insert(fx, v)
-- end

-- for _, v in ipairs(assets) do
--     table.insert(Assets, v)
-- end
